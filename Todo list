  Rev4 (This Revision)

  1. Fix Junos predefined application mapping — Built-in predefined apps like junos-ssh, junos-http, etc. are still generating custom objects in the final SRX config instead of referencing the
  predefined names. Trace through the full pipeline (parser → intermediate → converter) and perfect so predefined apps are never duplicated as custom objects.
  2. Security profile → UTM/IDP/SecIntel generation — PAN-OS/FortiGate security profiles are parsed and displayed but don't generate real SRX config. Emit actual set security utm, set security idp,
  and set security intelligence commands from source profiles.
  3. L2 interface and virtual-wire support — Research each vendor's L2/transparent/virtual-wire modes (PAN-OS virtual wire, FortiGate transparent mode, Cisco ASA transparent mode, SRX L2 mode),
  update all parsers, and generate correct SRX L2 mode configuration. Add L2 interface visibility to both from-FW and to-SRX interface/routing tabs.
  4. Rule optimization/consolidation — Extend the existing shadow detector to recommend rule merges and consolidations, not just flag shadows.

  Rev5

  5. Multi-firewall collapse into logical-systems/tenants — Import two or more firewall configs and collapse them into a single SRX using logical-systems or tenant systems. Guide the user on the
  differences between the two.

  Rev6

  6. Check Point, SonicWall, and Huawei parsers — Add parsers for Check Point (R80+), SonicWall (SonicOS), and Huawei USG/HiSecEngine.

  Rev7

  7. "I am new here" guided walkthrough — Dismissible bubble-style first-time user guide.
  8. Enhanced sanitization + mapping table — Strip corp branding, show mapping table, add branding to sample configs.
  9. Post-conversion diff view — Side-by-side source coverage vs generated SRX comparison.
  10. Save/load projects — Export/import full intermediate config + mappings + review state as JSON.
  11. Bulk rule operations — Multi-select for bulk accept, bulk edit, bulk delete.
  12. Greenfield templates — Pre-built starting configs for common deployments.
  13. Config syntax validation — Validate generated set commands via syntax checker or Junos commit-check via MCP.

  Blocked — Waiting on Vendor APIs

  14. Push to Security Director Cloud / SD On-Prem / Mist — Cannot implement until HPE Juniper exposes public APIs for SDC, Security Director on-prem, and Mist Cloud. Update the UI to inform users of
   this dependency rather than showing "coming soon."
