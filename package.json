{
  "name": "firewallintentconverter",
  "version": "1.2.0",
  "description": "Professional firewall configuration conversion tool — PAN-OS to Juniper SRX",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "build:standalone": "vite build --config vite.standalone.config.js && sed 's|\\.\\./assets/|./assets/|g' dist-standalone/standalone/index.html > dist-standalone/index.html && rm -rf dist-standalone/standalone && cp standalone/README.txt dist-standalone/",
    "zip:standalone": "npm run build:standalone && cd dist-standalone && zip -r ../firewallintentconverter-standalone.zip . -x 'prompts/*' && cd .."
  },
  "devDependencies": {
    "@fontsource-variable/geist": "5.2.9",
    "@fontsource-variable/geist-mono": "5.2.8",
    "@vitejs/plugin-react": "6.0.2",
    "acorn": "^8.17.0",
    "fast-xml-parser": "5.8.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "vite": "8.0.16",
    "vitest": "4.1.10"
  }
}
